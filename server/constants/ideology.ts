/** Kept so server/voting's imports keep working. The definition is shared/ideology.ts. */
export {
  IDEOLOGY_DIMENSIONS,
  type IdeologyDimension,
  emptyIdeologyVector,
  clampIdeologyValue,
} from '@shared/ideology';
